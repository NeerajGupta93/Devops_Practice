docker exec -it cecc5bdc1b98 bash
mysql -u root -p(mysql -u root -h db_host -p)
test@123
show databases;
create database KYC;
use KYC;

/// create table first ////
insert into messages (message) value ("lol");


/// Create tabel ///
use KYC
CREATE TABLE messages (
    id INT AUTO_INCREMENT PRIMARY KEY,
    message TEXT
);
